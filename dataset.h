#ifndef DATASET_H
#define DATASET_H

#include <vector>
#include <string>


class dataset
{
    public:
        vector<int> splitInput(string tempInput);
        void visDataSet(vector<int> inputList);

    protected:

    private:
};

#endif // DATASET_H
