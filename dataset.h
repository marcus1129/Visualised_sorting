#ifndef DATASET_H
#define DATASET_H

#include <vector>
#include <string>
#include <iostream>
using namespace std;


class dataset
{
    public:
        vector<int> splitInput(char tempInput[]);
        visDataSet(vector<int> inputList);

    protected:

    private:
};

#endif // DATASET_H
